You are a senior React + MUI5 engineer.

Render a pixel-perfect table using the provided JSON table_desktop.json and tt_desktop.png for visual reference.

STRICT RULES:
- Use CSS Grid (NOT flexbox)
- Use EXACT gridTemplateColumns values
- DO NOT modify spacing, padding, or sizes
- DO NOT add/remove columns
- Maintain exact alignment
- Use fixed row heights
- Map debit → error color, credit → success color

Implementation:
- Use MUI Box for grid container
- One grid for header and one for each row
- All rows must share identical gridTemplateColumns




